# OS
.DS_Store

# Playwright / tooling artifacts
**/.playwright-mcp/
*.log

# Editor
.vscode/
.idea/

# Strata data cache + outputs
data/
**/__pycache__/
*.pyc
.venv/
outputs/
.python-version

# Generated report data served by api/main.py (produced by scripts/build_reports.py).
# The Dockerfile copies api/reports/ from the build context at deploy time, so the
# deploy must regenerate these first; they are not source and stay out of git.
api/reports/

# Secrets — never commit
.env
.env.*

# Test + OS cruft
.pytest_cache/
.DS_Store
